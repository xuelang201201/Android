<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/btn_send_broadcast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="发出广播" />

    <Button
        android:id="@+id/btn_register_receiver"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btn_send_broadcast"
        android:layout_below="@+id/btn_send_broadcast"
        android:text="动态注册广播接收者" />

    <Button
        android:id="@+id/btn_unregister_receiver"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btn_register_receiver"
        android:layout_below="@+id/btn_register_receiver"
        android:text="取消注册广播接收者" />

    <Button
        android:id="@+id/btn_send_ordered_broadcast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btn_unregister_receiver"
        android:layout_below="@+id/btn_unregister_receiver"
        android:text="发送有序广播" />

</RelativeLayout>

